[package]
name = "boredgames"
version = "0.1.0"
edition = "2024"

[dependencies]
crossterm = {version = "0.29.0", features = ["serde"]}
raylib = { version = "5.5.0", features = ["with_serde"
] }
rmp-serde = "1.3.1"
serde = {version = "1.0.228", features = ["derive", "rc"]}
tokio = {version = "1.49.0", features = ["full"]}
[features]
net = []
